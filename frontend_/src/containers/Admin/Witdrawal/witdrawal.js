import React, {Component} from 'react';
import { Typography, Grid } from "@material-ui/core";

class Witdrawal extends Component{
    render(){
        return(
            <Grid item md={10} style={{paddingLeft: 10, boxSizing: 'border-box'}}>
                Admin Witdrawal
            </Grid>
        )
    }
}

export default Witdrawal;